version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: tupi_digital_db
    environment:
      POSTGRES_USER: tupidigital
      POSTGRES_PASSWORD: tupipassword
      POSTGRES_DB: tupi_digital
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
